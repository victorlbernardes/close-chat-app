import React from "react";
import { View } from "react-native";

export default function SeparatorItem({}) {
  return (
    <View style={{ height: 3, width: "100%", backgroundColor: "#FFD580" }} />
  );
}